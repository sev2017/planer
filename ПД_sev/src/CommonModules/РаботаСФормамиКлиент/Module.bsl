Процедура ОповеститьПослеЗаписиФормы(ИмяСобытия, Ссылка, стрПараметры = Неопределено) Экспорт 
	Параметры = Новый Структура;
	Параметры.Вставить("Ссылка", Ссылка);
	//стрПараметры.Вставить("Проект", Ссылка.Проект);
	Если Не стрПараметры = Неопределено Тогда
		Параметры.Вставить("Параметры", стрПараметры);	
	КонецЕсли;
	Оповестить(ИмяСобытия, Параметры, Ссылка); 
КонецПроцедуры