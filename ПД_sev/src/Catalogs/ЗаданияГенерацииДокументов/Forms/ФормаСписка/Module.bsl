
&НаКлиенте
Процедура ЗапуститьГенерацию(Команда)
	ЗапуститьГенерациюСервер();	
КонецПроцедуры

&НаСервере
Процедура ЗапуститьГенерациюСервер()
	ФоновыеЗадания.Выполнить("РегламентныеЗаданияСервер.ВыполнитьГенерацияДокументов");
КонецПроцедуры // ЗапуститьГенерациюСервер()
